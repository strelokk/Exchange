apply plugin: 'java'

dependencies {
    compile('de.greenrobot:DaoGenerator:1.3.0')
}

sourceSets {
    main {
        java.srcDirs = ['src']
        resources.srcDirs = ['src']
        aidl.srcDirs = ['src']
        renderscript.srcDirs = ['src']
    }
}